[phases.setup]
nixPkgs = ["dotnet-sdk_8"]

[phases.build]
cmds = [
  "dotnet restore HRSystem.sln",
  "dotnet publish HRSystem.API/HRSystem.API/HRSystem.API.csproj -c Release -o /app/publish"
]

[start]
cmd = "dotnet /app/publish/HRSystem.API.dll"